// Dissertation Workbench browser code is split into ordered classic scripts under /app/.
// Keep this file as a compatibility pointer for tooling that still requests /app.js.
// See public/app/events.js for startup and event binding. fetch;
